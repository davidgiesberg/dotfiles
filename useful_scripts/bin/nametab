#!/bin/bash
NEW_NAME=$1

if [ -z "$NEW_NAME" ]; then
  echo "usage: nametab 'Tab Name'"
  exit
fi

# Detect if we're in a tmux session or not
if { [ "$TERM" = "screen" ] && [ -n "$TMUX" ]; } then
  tmux rename-window "$NEW_NAME"
else
  echo -n -e "\033]0;$NEW_NAME\007"
fi